#!groovy
// Check nnavm191
properties([disableConcurrentBuilds()])

pipeline {
    agent { 
        label 'master'
        }
  stages {
        stage("First step") {
            steps {
                echo "Hello Worldd"
            }
        }
        
    }
}
