Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                sh 'ssh root@10.122.0.195 \'uptime\''
            }
        }
    }
}
